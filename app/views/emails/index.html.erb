<div class="container text-center">
<%= link_to "Recevoir un email", emails_path, method: "post", class: "btn btn-primary btn-lg", remote: true %>
<br><br>
  <div class="container px-lg-5" id="email-list">
    <div class="row mx-lg-n5">
      <div class="col py-3 px-lg-5 border bg-light">Liste des emails</div>
      <div class="col py-3 px-lg-5 border bg-light">Email selectionné</div>
    </div>

    <% @emails.all.each do |email| %>
      <%= render 'email', email: email %>
    <% end %>
  </div>
</div>

